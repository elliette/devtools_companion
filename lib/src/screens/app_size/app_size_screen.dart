import 'package:flutter/widgets.dart';

import '../../shared/widgets/todo_screen.dart';

class AppSizeScreen extends StatelessWidget {
  const AppSizeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const TodoScreen(screenName: 'App Size');
  }
}